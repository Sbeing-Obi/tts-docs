---
title: 2026-07-01 — Project kickoff
type: devlog
date: 2026-07-01
tags: [devlog]
---

# 2026-07-01 — Project kickoff & Phase 0

## What I did

- Researched the mid-2026 TTS landscape (open models, commercial APIs, serving/infra,
  safety/legal) and the documentation toolchain (Diátaxis, ADRs, Obsidian, Quartz, Markdown
  portability).
- Wrote and approved the 7-phase plan.
- **Phase 0 — code:** scaffolded `tts-service` (FastAPI `GET /health`, pytest, `pyproject.toml`).
  Verified: `pytest` green and `uvicorn` serves `/health` → `{"status":"ok",...}`.
- **Phase 0 — docs:** created this vault, seeded ADR-0001…0005, wrote the
  [codec-LM TTS explanation](../explanation/how-codec-lm-tts-works.md), four templates, and
  the changelog.

## Why

- Self-host open MIT/Apache models (own the stack + cloning); real-time first; both cloning
  tiers; strict portable docs. Reasoning captured in the ADRs.

## What I learned / gotchas

- The machine's default Python is **3.14**, but I used **Python 3.11** for the venv on
  purpose: PyTorch + Chatterbox/Kokoro (Phase 1+) reliably support 3.11, so one venv serves
  the whole project. Avoids a second env later.
- FastAPI's `TestClient` emitted a `StarletteDeprecationWarning` suggesting `httpx2`. Harmless
  for now; revisit if it becomes an error on a future upgrade.
- Quartz v5 installs its community plugins via **symlinks**, which fail on Windows
  (`EPERM ... symlink`) without Developer Mode / admin. Not a real blocker — the GitHub
  Actions deploy builds on Linux where symlinks work. Documented in the publish how-to.

## Phase 1 — Hello, Voice (done, same day)

- Built the model-agnostic `TTSEngine` interface (`engine/base.py`), a `KokoroEngine`
  (kokoro-onnx, CPU), and OpenAI-compatible `POST /v1/audio/speech`. 4 pytest tests green;
  verified end-to-end (text → valid WAV).
- **What I learned:** `kokoro-onnx` (onnxruntime) avoids PyTorch *and* the manual espeak-ng
  install on Windows — the English path just worked. CPU RTF ≈ 2.2 (int8): fine for
  one-shot, too slow for real-time — a Phase 2 concern. Model weights (~115 MB) are a
  manual download (not pip), kept in git-ignored `models/`.

## Direction — multilingual Indian TTS (ADR-0007)

- Chatterbox zero-shot on my own voice worked (sounds like me, decent), but English base →
  English accent on "Namaste". Realized Indic pronunciation is a language-coverage axis,
  not voice-identity — fine-tuning my English voice wouldn't fix it.
- Product goal set: cover Indian languages + cloning. Research picked a **two-model stack**:
  **IndicF5 (MIT, 11 langs, zero-shot cloning)** + **Indic Parler-TTS (Apache, coverage)**,
  routed by language behind the existing `TTSEngine`. **No fine-tuning for v1.**

## Next

- Validate **IndicF5** zero-shot on my voice speaking Hindi (Colab notebook).
- Then implement `IndicF5Engine` + an `IndicRouterEngine` behind the API; add voice enrollment.
- (Deferred) fine-tuning per [fine-tune your voice](../how-to/fine-tune-your-voice.md) only for Tier-2 cloning / quality.
- Push both repos to GitHub (personal account) + enable Pages so the docs site goes live.
- Phase 2: streaming `WS /v1/realtime` + time-to-first-audio budget (< 300 ms).
